# Outlook 365 Setup (Windows/macOS)
1) Add Account → sign in → approve MFA → allow sync.
2) If stuck: clear cached credentials (Windows Credential Manager / macOS Keychain).
3) Verify M365 license is active.
4) Large mailboxes may take hours to fully sync.
